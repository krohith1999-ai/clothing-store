# Supabase database (PostgreSQL)
# Get these from: Supabase Dashboard → Project Settings → Database
# Use "Connection string" → "URI" and copy host, or build:
#   jdbc:postgresql://db.PROJECT_REF.supabase.co:5432/postgres
# Add ?sslmode=require for Supabase.

SUPABASE_DB_URL=jdbc:postgresql://db.YOUR_PROJECT_REF.supabase.co:5432/postgres?sslmode=require
SUPABASE_DB_USER=postgres
SUPABASE_DB_PASSWORD=your-database-password
